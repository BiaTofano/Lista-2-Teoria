Resposta:
int randomValues[5]; 

void setup() { 
Serial.begin(9600); 

randomSeed(analogRead(0)); 
for (int i = 0; i < 5; i++) { 
randomValues[i] = random(1, 101); 
Serial.println(randomValues[i]); 
} 
} 
void loop() { 

}
